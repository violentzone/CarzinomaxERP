# Base URL for the Carzinomax ERP API.
# In dev this stays as `/api/v1` — Vite proxies `/api` to http://localhost:8000
# (see vite.config.js). For a production build, point this at your deployed API,
# e.g. VITE_API_BASE_URL=https://erp.example.com/api/v1
VITE_API_BASE_URL=/api/v1
